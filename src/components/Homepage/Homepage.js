import React from 'react';
import Header from '../Header/Header';
import './homepage.scss';

const Homepage = () => {
    return (
        <div>
            <Header/>
        </div>
    );
};

export default Homepage;